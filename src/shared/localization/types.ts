export interface Translations {
  auth: {
    appName: string;
    createPin: string;
    confirmPin: string;
    enterPin: string;
  };
  settings: {
    title: string;
    preferences: string;
    darkMode: string;
  };
  exercises: {
    title: string;
    empty: string;
  };
  workout: {
    title: string;
    empty: string;
  };
  navigation: {
    workoutsTab: string;
    exercisesTab: string;
    settingsTab: string;
  };
}
